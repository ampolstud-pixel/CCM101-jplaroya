### docker ps

The docker ps command displays the containers that are currently running.

---

### docker stop nginx-server

The docker stop command stops the running Nginx container.

### docker ps

The docker ps command shows currently running containers.

### docker ps -a

The docker ps -a command displays both running and stopped containers.
